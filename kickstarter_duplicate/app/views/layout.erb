    <div class="container">
      <% if session[:user_id] != nil %>
      Welcome <%= @donor.username %>! | <a href="/logout">Logout</a>
      <% else %>
        <a href="/login">Login</a> or <a href="/signup">sign up</a> to tweet!
      <% end %>

      <%= yield %>
      
       </div>